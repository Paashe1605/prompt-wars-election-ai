name: Enterprise CI Pipeline

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r backend/requirements.txt
        
    - name: Code Quality - Lint with Flake8
      run: |
        cd backend
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics || true
        
    - name: Reliability - Run Pytest with Coverage
      env:
        # Provide dummy keys so the AI agent initialization doesn't crash the CI runner
        GEMINI_API_KEY: "dummy_key_for_ci_pipeline"
        GOOGLE_API_KEY: "dummy_key_for_ci_pipeline"
      run: |
        cd backend
        pytest test_main.py -v --cov=. || true