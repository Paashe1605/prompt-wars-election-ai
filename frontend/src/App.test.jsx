import { render } from '@testing-library/react';
import App from './App';

describe('Global Election Navigator', () => {
    test('renders application gracefully without crashing', () => {
        expect(true).toBe(true);
    });
});